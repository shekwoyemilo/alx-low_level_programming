#include "main.h"

/**
 * _isupper - checks for uppercase
 * @c: The number to be checked
 * 
 * Return 1 for uppercase letter or 0 otherwise
 */

int _putchar(char c)

{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
