#include "main.h"

/**
 * multiply - multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply with the first
 * Return: The results of multiplication between parameter a and parameter b
 */

int mul(int a, int b)
{
	return (a * b);
}
